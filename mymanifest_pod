apiVersion: v1
kind: Pod
metadata:
  name: myapp image deployment
  labels:
    role: myrole
spec:
  containers:
    - name: web
      image: myapp
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
